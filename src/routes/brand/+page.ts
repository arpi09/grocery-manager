import type { PageLoad } from './$types';

export const load: PageLoad = () => ({
	meta: {
		title: 'Skaffu — varumärkespalett',
		description: 'Aktiv Fresh-palett, semantiska tokens och komponentgalleri.',
		noindex: true
	}
});
